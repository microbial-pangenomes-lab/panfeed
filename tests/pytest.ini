[pytest]
minversion = 6.0
addopts = -ra -q --color=yes --showlocals
testpaths = 
    tests/unit
    tests/integration

[tool.pytest.ini_options]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Disable warnings
filterwarnings = 
    ignore::DeprecationWarning
